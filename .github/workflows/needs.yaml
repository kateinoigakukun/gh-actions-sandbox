name: Check needs
on: [push]
jobs:
  dep-x:
    runs-on: ubuntu-latest
    if: false
    steps:
    - run: echo "Hello"
  needs-x:
    runs-on: ubuntu-latest
    needs: [dep-x]
    if: ${{ always() }}
    steps:
    - run: echo "Hello"
